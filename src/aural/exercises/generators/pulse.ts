import { Question, StageNumber } from '../../curriculum/types'

/**
 * Generator for pulse clapping exercises (A)
 * The candidate should clap the pulse of a piece played by the examiner
 */
export const createPulseQuestions = (
  count: number,
  stage: StageNumber
): Question[] => {
  // TODO: Implement pulse clapping question generation
  return []
}

