import { Question, StageNumber } from '../../curriculum/types'

/**
 * Generator for rhythm echo clapping exercises (B)
 * The candidate should clap back the rhythm of phrases as 'echoes'
 */
export const createRhythmQuestions = (
  count: number,
  stage: StageNumber
): Question[] => {
  // TODO: Implement rhythm echo clapping question generation
  return []
}

