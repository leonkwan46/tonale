import type { Question, StageNumber } from '../../curriculum/types'

/**
 * Create dynamics identification questions.
 * NOT YET IMPLEMENTED - Future feature for identifying loud/quiet, smooth/detached.
 *
 * @param count - Number of questions to generate
 * @param stage - Stage number (difficulty level)
 * @returns Empty array (not yet implemented)
 */
export const createDynamicsQuestions = (
  count: number,
  stage: StageNumber
): Question[] => {
  console.warn('Dynamics exercises not yet implemented')
  return []
}
